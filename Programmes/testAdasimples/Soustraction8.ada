with Ada.Text_IO;use Ada.Text_IO;

procedure Soustraction8 is

    i: Integer;
    y: Integer;
    z: Integer;

begin
    z:= 5;
    y:= 3;
    i := -1 - z; --(-6)
    Put(z);
    Put(y);
    Put(i);
end Soustraction8;